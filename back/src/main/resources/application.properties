spring.datasource.url=jdbc:mysql://localhost:3306/mdd

# TODO change user/password for an non admin account
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

server.port=3001
server.servlet.contextPath=/api

springdoc.swagger-ui.path=/documentation.html

rsa.private-key=classpath:certs/private.pem
rsa.public-key=classpath:certs/public.pem

#TODO add to readme ! 

#How launch mysql server
#cd C:\Program Files\MySQL\MySQL Server 8.0\bin
# mysql -u root -D mdd -p
#quit

#How generate rsa keys (see P3!)

# Add drive files for DB and API ! *maybe not all on back !